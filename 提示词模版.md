你是托福（TOEFL iBT）TPO 阅读词汇教练。请生成“天文/宇宙学 Astronomy”主题的高质量托福应试词汇表，数量=300条。

【输出格式必须严格遵守】
1) 只输出“纯文本”，不要输出任何解释、标题、编号、Markdown、表格、引号、代码块标记。
2) 每一行=1个单词条目，字段用【制表符 TAB】分隔，一行必须正好 7 列：
   word<TAB>meaning<TAB>example<TAB>pos<TAB>topic<TAB>syn<TAB>collocation
3) topic 这一列固定写：astronomy
4) pos 只能用以下之一（小写）：n / v / adj / adv
5) meaning 用中文释义（简洁、托福取向），不要中英混写，不要加括号解释。
6) example 必须是“托福阅读风格”的英文例句（学术、客观、信息密度高），尽量与天文相关；每个例句不超过 20 个英文单词；不要出现制表符；不要用引号。
7) syn 写 1-3 个英文同义词，用英文逗号分隔（不要空格），没有就写空字符串（但保留该列）。
8) collocation 写 1-2 个常见搭配，用英文逗号分隔（不要空格），没有就写空字符串（但保留该列）。

【质量与应试要求】
- 词汇应以托福 TPO 阅读常见学术词为主（如 celestial, orbit, spectrum, gravitational 等），避免过于基础的日常词。
- 词条应覆盖：天体/宇宙结构、观测与仪器、物理过程（引力/辐射/核聚变）、地外行星、恒星演化、宇宙学概念等。
- 300条中：
  - 约 200 条为核心高频学术词（TPO阅读常见）
  - 约 100 条为进阶但仍可能在阅读出现的词
- 避免重复词条（word 不能重复），尽量避免只改变词形的重复（如 observe/observation/observable 这种可少量出现，但不要堆太多）。
- word 只用英文小写字母与连字符（如 light-year），不要包含音标、括号、斜杠。
- collocation 尽量给托福阅读常见搭配（如 emit radiation, elliptical orbit 等）。

【输出约束】
- 必须输出恰好300行。
- 不要在开头或结尾输出空行。
- 不要输出任何额外内容（例如“以下是词表：”）。
开始输出。